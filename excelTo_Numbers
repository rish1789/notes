function excelTo_Numbers(comb){
  let result =0;
  let l=comb.length;
  for( let  i=l-1;i>=0;i--){
    let multipiler=Math.pow(26,l-i-1);
    result=result+(comb[i].charCodeAt(0)-'A'.charCodeAt(0)+1)*multipiler;
  }
  return result;
}
console.log(excelTo_Numbers(`AA`));
